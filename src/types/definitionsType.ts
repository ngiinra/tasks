export type ListType = {
  text: string;
  value: string;
  id?: number;
  userId: string;
  active: 0 | 1; //bit
};

export type DefinitionType = {
  text: string;
  value: string;
  id?: number;
  userId: string;
  active: 0 | 1; //bit
};
